# Copy to .env and fill in with real values from the Entra ID app registrations (Part 0).
# .env is gitignored — never commit real tenant/client IDs here.
TENANT_ID=YOUR_TENANT_ID
API_CLIENT_ID=YOUR_API_APP_CLIENT_ID
SPA_CLIENT_ID=YOUR_SPA_APP_CLIENT_ID
API_BASE_URL=http://localhost:8080/api
